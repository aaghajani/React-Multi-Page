import React from "react";
import NotFoundComponent from "../components/section/notFoundComponent";

const NotFound = (props) => {
  return (
    <React.Fragment>
      <main className="container">
        <NotFoundComponent />
      </main>
    </React.Fragment>
  );
};
export default NotFound;
